version     = "0.1.0"
author      = "daveey"
description = "Matrix Games: eight cogs, a yard full of tokens, and one payoff matrix that changes everything."
license     = "MIT"

srcDir = "src"

requires "nim >= 2.2.4"
requires "bitworld >= 0.1.0"
requires "mummy >= 0.4.7"
requires "curly >= 1.1.1"
requires "whisky"
